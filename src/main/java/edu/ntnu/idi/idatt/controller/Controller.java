package edu.ntnu.idi.idatt.controller;

/**
 * The Controller interface is used to define the basic structure of a controller in the application.
 * It includes an initialize method that can be overridden by implementing classes.
 */
public interface Controller {
  private void initialize() {}
}
